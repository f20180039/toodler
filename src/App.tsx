import { FlowBuilderPage } from './routes/FlowBuilderPage'

/** One screen: the flow diagram, the node types that build it, and the
 *  parameters of whichever step is selected. */
export function App() {
  return <FlowBuilderPage />
}
